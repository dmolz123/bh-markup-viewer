{
  "name": "bh-markup-viewer",
  "version": "1.0.0",
  "private": true,
  "description": "Bluebeam Studio markup deep-link viewer — overlays session markups on the page snapshot by rect, deep-links to any single markup, opens the Session in Revu. Backend proxy holds the Studio Bearer token so the browser never touches the API directly.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "license": "MIT"
}
